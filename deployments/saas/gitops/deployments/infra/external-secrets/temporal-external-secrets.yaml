---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: temporal-db-credentials
  namespace: saas-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault
    kind: SecretStore
  target:
    name: temporal-db-credentials
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: temporal-db-credentials
        property: password
    - secretKey: username
      remoteRef:
        key: temporal-db-credentials
        property: username
