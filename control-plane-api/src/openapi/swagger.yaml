openapi: 3.0.0
components:
  examples: {}
  headers: {}
  parameters: {}
  requestBodies: {}
  responses: {}
  schemas:
    CreatedByInfo:
      properties:
        uid:
          type: string
        email:
          type: string
        externalId:
          type: string
      required:
        - uid
        - email
      type: object
      additionalProperties: false
    WorkspaceResponse:
      properties:
        uid:
          type: string
          description: Workspace unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        name:
          type: string
          description: Workspace name
          example: Example workspace
        description:
          type: string
          nullable: true
          description: Workspace description
          example: Example workspace description
        status:
          type: string
        createdAt:
          type: string
          format: date-time
        createdBy:
          $ref: "#/components/schemas/CreatedByInfo"
        updatedAt:
          type: string
          format: date-time
      required:
        - uid
        - name
        - status
        - createdAt
        - createdBy
        - updatedAt
      type: object
      additionalProperties: false
    PaginationInfo:
      properties:
        currentPage:
          type: number
          format: double
        totalPages:
          type: number
          format: double
        totalData:
          type: number
          format: double
        limit:
          type: number
          format: double
        hasNextPage:
          type: boolean
        hasPreviousPage:
          type: boolean
      required:
        - currentPage
        - totalPages
        - totalData
        - limit
        - hasNextPage
        - hasPreviousPage
      type: object
      additionalProperties: false
    ListWorkspacesResponse:
      properties:
        data:
          items:
            $ref: "#/components/schemas/WorkspaceResponse"
          type: array
        pagination:
          $ref: "#/components/schemas/PaginationInfo"
        serverTime:
          type: string
      required:
        - data
        - pagination
      type: object
      additionalProperties: false
    ValidateErrorJSON:
      properties:
        message:
          type: string
          enum:
            - Validation failed
          nullable: false
        details:
          properties: {}
          additionalProperties: {}
          type: object
      required:
        - message
        - details
      type: object
      additionalProperties: false
    CreateWorkspaceRequestBody:
      properties:
        name:
          type: string
          description: Workspace name
          example: Example workspace
        description:
          type: string
          nullable: true
          description: Workspace description
          example: Example workspace description
        extAccountId:
          type: string
          description: External account ID
        extWorkspaceId:
          type: string
          description: External workspace ID
          example: w-123456789
      required:
        - name
        - extAccountId
      type: object
      additionalProperties: false
    GenerateBootstrapTokenResponse:
      description: Response for generating a new bootstrap token
      properties:
        workspaceUid:
          type: string
          description: Workspace unique ID
        extWorkspaceId:
          type: string
          description: External workspace ID
        token:
          type: string
          description: New bootstrap token
        expiredAt:
          type: string
          format: date-time
          description: Token expiration date
        agentStatus:
          type: string
          description: Agent status after regeneration
      required:
        - workspaceUid
        - extWorkspaceId
        - token
        - expiredAt
        - agentStatus
      type: object
      additionalProperties: false
    PlatformProviderRegionResponse:
      properties:
        uid:
          type: string
          description: Unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        name:
          type: string
          description: Region name
          example: ap-southeast-1
        displayName:
          type: string
          description: Region display name
          example: Singapore
      required:
        - uid
        - name
        - displayName
      type: object
      additionalProperties: false
    PlatformProviderWithRegionsResponse:
      properties:
        uid:
          type: string
          description: Unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        name:
          type: string
          description: Platform provider name
          example: AWS_EKS
        displayName:
          type: string
          description: Platform provider display name
          example: Amazon Elastic Kubernetes Service (EKS)
        regions:
          items:
            $ref: "#/components/schemas/PlatformProviderRegionResponse"
          type: array
      required:
        - uid
        - name
        - displayName
        - regions
      type: object
      additionalProperties: false
    ListPlatformProvidersResponse:
      properties:
        data:
          items:
            $ref: "#/components/schemas/PlatformProviderWithRegionsResponse"
          type: array
        pagination:
          $ref: "#/components/schemas/PaginationInfo"
        serverTime:
          type: string
      required:
        - data
        - pagination
      type: object
      additionalProperties: false
    ListPlatformProviderRegionsResponse:
      properties:
        data:
          items:
            $ref: "#/components/schemas/PlatformProviderRegionResponse"
          type: array
        pagination:
          $ref: "#/components/schemas/PaginationInfo"
        serverTime:
          type: string
      required:
        - data
        - pagination
      type: object
      additionalProperties: false
    HealthStatus:
      type: string
      enum:
        - ok
        - error
        - unknown
    HealthCheckResponse:
      properties:
        status:
          $ref: "#/components/schemas/HealthStatus"
        uptime:
          type: number
          format: double
        timestamp:
          type: number
          format: double
        services:
          properties:
            redis:
              $ref: "#/components/schemas/HealthStatus"
            keycloak:
              $ref: "#/components/schemas/HealthStatus"
          required:
            - redis
            - keycloak
          type: object
      required:
        - status
        - uptime
        - timestamp
        - services
      type: object
      additionalProperties: false
    AgentMTLSCredentials:
      description: mTLS credentials for agent communication
      properties:
        caCert:
          type: string
          description: CA certificate in PEM format (required for self-signed, optional
            for step-ca/aws-pca)
        clientCert:
          type: string
          description: Client certificate in PEM format
        clientKey:
          type: string
          description: Client private key in PEM format
        expiresAt:
          type: string
          format: date-time
          description: Certificate expiration date
        certSerialNumber:
          type: string
          description: Certificate serial number (hex string) for tracking/revocation
        certFingerprint:
          type: string
          description: Certificate fingerprint (SHA-256 of DER)
        caProvider:
          type: string
          enum:
            - self-signed
            - step-ca
            - aws-pca
          description: CA provider type that issued the certificate
      required:
        - clientCert
        - clientKey
        - expiresAt
      type: object
      additionalProperties: false
    AgentRegisterResponse:
      description: Response for successful agent registration
      properties:
        agentUid:
          type: string
          description: Agent unique ID
        workspaceUid:
          type: string
          description: Workspace unique ID
        extWorkspaceId:
          type: string
          description: External workspace ID
        mtls:
          $ref: "#/components/schemas/AgentMTLSCredentials"
          description: mTLS credentials for secure communication
      required:
        - agentUid
        - workspaceUid
        - extWorkspaceId
        - mtls
      type: object
      additionalProperties: false
    AgentRegisterRequest:
      description: Request body for agent registration
      properties:
        extWorkspaceId:
          type: string
          description: External workspace ID
        token:
          type: string
          description: Bootstrap token for agent registration
          example: abc123xyz...
      required:
        - extWorkspaceId
        - token
      type: object
      additionalProperties: false
    Record_string.unknown_:
      properties: {}
      additionalProperties: {}
      type: object
      description: Construct a type with a set of properties K of type T
    WorkspaceAppConfig:
      description: Workspace app configuration returned to agent
      properties:
        uid:
          type: string
          description: App unique ID
        name:
          type: string
          description: App name
        version:
          type: string
          description: App version
        status:
          type: string
          description: App status
        config:
          items:
            $ref: "#/components/schemas/Record_string.unknown_"
          type: array
          description: App configuration entries
        parameter:
          $ref: "#/components/schemas/Record_string.unknown_"
          description: App parameters
      required:
        - uid
        - name
        - version
        - status
        - config
        - parameter
      type: object
      additionalProperties: false
    CreatedByRef:
      description: Created-by user reference
      properties:
        uid:
          type: string
          description: User unique ID
        email:
          type: string
          description: User email
        fullName:
          type: string
          description: User full name
      required:
        - uid
        - email
        - fullName
      type: object
      additionalProperties: false
    AgentSyncResponse:
      description: Response for agent sync endpoint — workspace configuration returned
        to agent
      properties:
        uid:
          type: string
          description: Workspace unique ID
        extWorkspaceId:
          type: string
          description: External workspace ID
        name:
          type: string
          description: Workspace name
        description:
          type: string
          description: Workspace description
        status:
          type: string
          description: Current workspace status
        workspaceApps:
          items:
            $ref: "#/components/schemas/WorkspaceAppConfig"
          type: array
          description: Workspace apps configuration
        storage:
          $ref: "#/components/schemas/Record_string.unknown_"
          description: Storage configuration
        network:
          $ref: "#/components/schemas/Record_string.unknown_"
          description: Network configuration
        createdAt:
          type: string
          format: date-time
          description: Workspace creation timestamp
        createdBy:
          $ref: "#/components/schemas/CreatedByRef"
          description: User who created the workspace
        updatedAt:
          type: string
          format: date-time
          description: Last update timestamp
      required:
        - uid
        - extWorkspaceId
        - name
        - description
        - status
        - workspaceApps
        - storage
        - network
        - createdAt
        - createdBy
        - updatedAt
      type: object
      additionalProperties: false
    AgentStatusReport:
      description: Agent status report sent during sync
      properties:
        healthy:
          type: boolean
          description: Whether the agent is healthy
        message:
          type: string
          description: Optional status message
      required:
        - healthy
      type: object
      additionalProperties: false
    AgentSyncRequest:
      description: Request body for agent sync endpoint
      properties:
        agentVersion:
          type: string
          description: Agent software version
        kubernetesVersion:
          type: string
          description: Kubernetes cluster version
        nodeCount:
          type: number
          format: double
          description: Number of nodes in the cluster
        status:
          $ref: "#/components/schemas/AgentStatusReport"
          description: Agent health status
      type: object
      additionalProperties: false
    PrincipalResponse:
      properties:
        uid:
          type: string
          description: Unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        externalId:
          type: string
          description: |-
            Principal external ID
            Examples: user:123 | agent:cluster-1
          example: user:joe@example.com
        kind:
          type: string
          description: Principal kind
          example: user
        email:
          type: string
          description: Principal email
          example: joe@example.com
        createdAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
      required:
        - uid
        - externalId
        - kind
        - email
        - createdAt
      type: object
      additionalProperties: false
    ListPrincipalsResponse:
      properties:
        data:
          items:
            $ref: "#/components/schemas/PrincipalResponse"
          type: array
        pagination:
          $ref: "#/components/schemas/PaginationInfo"
        serverTime:
          type: string
      required:
        - data
        - pagination
      type: object
      additionalProperties: false
    AccountPlanEnum:
      enum:
        - free
        - enterprise
      type: string
    PlatformProviderResponse:
      properties:
        uid:
          type: string
          description: Unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        name:
          type: string
          description: Platform provider name
          example: AWS_EKS
        displayName:
          type: string
          description: Platform provider display name
          example: Amazon Elastic Kubernetes Service (EKS)
      required:
        - uid
        - name
        - displayName
      type: object
      additionalProperties: false
    AccountResponse:
      properties:
        uid:
          type: string
          description: Unique ID
          example: 83ef9fc3-159c-43fc-a31f-0d4575dc373c
        extAccountId:
          type: string
          description: External account ID
          example: abc1234
        name:
          type: string
          description: Account name
          example: Example Account
        plan:
          $ref: "#/components/schemas/AccountPlanEnum"
        platformProvider:
          $ref: "#/components/schemas/PlatformProviderResponse"
        platformProviderRegion:
          $ref: "#/components/schemas/PlatformProviderRegionResponse"
        status:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        createdBy:
          $ref: "#/components/schemas/CreatedByInfo"
      required:
        - uid
        - name
        - plan
        - platformProvider
        - platformProviderRegion
        - status
        - createdAt
        - updatedAt
        - createdBy
      type: object
      additionalProperties: false
    ListAccountsResponse:
      properties:
        data:
          items:
            $ref: "#/components/schemas/AccountResponse"
          type: array
        pagination:
          $ref: "#/components/schemas/PaginationInfo"
        serverTime:
          type: string
      required:
        - data
        - pagination
      type: object
      additionalProperties: false
    ProvisionAccountRequestBody:
      properties:
        initialAccountOwnerEmail:
          type: string
          description: Initial account owner email
          example: joe@example.com
        createInitialAccountOwner:
          type: boolean
          description: Create initial account owner
          example: true
        extAccountId:
          type: string
          description: External account ID
        accountPlan:
          $ref: "#/components/schemas/AccountPlanEnum"
          description: Account plan
        accountName:
          type: string
          description: Account name
          example: ACME Corp
        platformProviderRegionUid:
          type: string
          description: Region unique ID
          example: 1fa42981-f87d-4cec-8ebe-3eb262037ce3
        platformProviderUid:
          type: string
          description: Platform provider unique ID
          example: 8ad0b3a3-3b3c-47ba-99ec-754dcf09a5b1
      required:
        - initialAccountOwnerEmail
        - accountPlan
        - platformProviderUid
      type: object
  securitySchemes:
    bearerAuth:
      type: apiKey
      name: Authorization
      in: header
    pkceSSO:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: http://localhost:5980/realms/controlplane/protocol/openid-connect/auth
          tokenUrl: http://localhost:5980/realms/controlplane/protocol/openid-connect/token
          scopes:
            openid: OpenID Connect scope
            profile: Access to your profile information
            email: Access to your email
info:
  title: sparqd-control-plane-api
  description: API documentation for the control plane service
  version: 1.0.0
  license:
    name: ISC
paths:
  /accounts/{accountUid}:
    get:
      operationId: GetAccount
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/AccountResponse"
                nullable: true
      description: |-
        Retrieves the detail of an account.
        Supply the unique account UID.
      summary: Get account detail
      tags:
        - Accounts
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: path
          name: accountUid
          required: true
          schema:
            type: string
  /workspaces:
    get:
      operationId: ListWorkspaces
      responses:
        "200":
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListWorkspacesResponse"
      description: Retrieves the list of workspaces.
      summary: List workspaces
      tags:
        - Workspaces
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: query
          name: _filters
          required: false
          schema:
            type: string
        - in: query
          name: _sort
          required: false
          schema:
            type: string
        - in: query
          name: _order
          required: false
          schema:
            type: string
        - in: query
          name: page
          required: false
          schema:
            format: double
            type: number
        - in: query
          name: limit
          required: false
          schema:
            format: double
            type: number
    post:
      operationId: CreateWorkspace
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/WorkspaceResponse"
                nullable: true
        "400":
          description: Validation Failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateErrorJSON"
      description: |-
        Create a workspace.
        Supply the workspace create input body.
      summary: Create workspace
      tags:
        - Workspaces
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWorkspaceRequestBody"
  /workspaces/{workspaceUid}:
    get:
      operationId: GetWorkspace
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/WorkspaceResponse"
                nullable: true
      description: |-
        Retrieves the detail of a workspace.
        Supply the unique workspace UID.
      summary: Get workspace detail
      tags:
        - Workspaces
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: path
          name: workspaceUid
          required: true
          schema:
            type: string
  /workspaces/{workspaceUid}/generateBootstrapToken:
    post:
      operationId: GenerateBootstrapToken
      responses:
        "200":
          description: Token Generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateBootstrapTokenResponse"
        "400":
          description: Validation Failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateErrorJSON"
      description: >-
        Generate a new bootstrap token for a workspace.

        Invalidates the existing token and resets agent status to
        PendingRegistration.
      summary: Generate new bootstrap token
      tags:
        - Workspaces
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: path
          name: workspaceUid
          required: true
          schema:
            type: string
  /admin/accounts:
    get:
      operationId: ListAccounts
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListAccountsResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: Retrieves the list of accounts.
      summary: List accounts
      tags:
        - Admin API
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: query
          name: _filters
          required: false
          schema:
            type: string
        - in: query
          name: _sort
          required: false
          schema:
            type: string
        - in: query
          name: _order
          required: false
          schema:
            type: string
        - in: query
          name: page
          required: false
          schema:
            format: double
            type: number
        - in: query
          name: limit
          required: false
          schema:
            format: double
            type: number
    post:
      operationId: ProvisionAccount
      responses:
        "201":
          description: ""
          content:
            application/json:
              schema:
                allOf:
                  - $ref: "#/components/schemas/AccountResponse"
                nullable: true
      description: Provision new account
      summary: Provision new account
      tags:
        - Admin API
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProvisionAccountRequestBody"
  /admin/principals:
    get:
      operationId: ListPrincipals
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPrincipalsResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: Retrieves the list of principals.
      summary: List principals
      tags:
        - Admin API
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: query
          name: _filters
          required: false
          schema:
            type: string
        - in: query
          name: _sort
          required: false
          schema:
            type: string
        - in: query
          name: _order
          required: false
          schema:
            type: string
        - in: query
          name: page
          required: false
          schema:
            format: double
            type: number
        - in: query
          name: limit
          required: false
          schema:
            format: double
            type: number
  /agent/register:
    post:
      operationId: RegisterAgent
      responses:
        "200":
          description: Registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRegisterResponse"
        "400":
          description: Validation Failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidateErrorJSON"
        "401":
          description: Invalid or expired token
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "403":
          description: Agent is suspended
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "409":
          description: Agent already registered
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "410":
          description: Agent has been deleted
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: >-
        Register a workspace cluster agent using a bootstrap token.

        This endpoint is unauthenticated - the bootstrap token serves as the
        credential.

        Upon successful registration, the agent receives mTLS certificates for
        secure communication.
      summary: Register agent
      tags:
        - Agent
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentRegisterRequest"
  /agent/sync:
    post:
      operationId: SyncAgent
      responses:
        "200":
          description: Synced
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentSyncResponse"
        "401":
          description: Invalid or missing certificate
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "403":
          description: Agent is suspended or not registered
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "404":
          description: Agent not found
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
        "410":
          description: Agent has been deleted
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: >-
        Sync agent state with control plane.

        Agent sends telemetry/status and receives workspace configuration.

        This endpoint requires mTLS authentication - the agent identity is
        extracted from the client certificate.
      summary: Sync agent
      tags:
        - Agent
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AgentSyncRequest"
  /health:
    get:
      operationId: GetHealthCheck
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthCheckResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: Retrieves the API health checks.
      summary: API Health Checks
      tags:
        - Health
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
  /platformProviders:
    get:
      operationId: ListPlatformProviders
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPlatformProvidersResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: Retrieves the list of supported platform providers.
      summary: List platform providers
      tags:
        - Platform Providers
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters: []
  /platformProviders/{platformProviderUid}/regions:
    get:
      operationId: ListPlatformProviderRegions
      responses:
        "200":
          description: Ok
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListPlatformProviderRegionsResponse"
        "500":
          description: Internal Server Error
          content:
            application/json:
              schema:
                properties:
                  message:
                    type: string
                required:
                  - message
                type: object
      description: Retrieves the list of supported regions of a platform provider.
      summary: List platform provider regions
      tags:
        - Platform Providers
      security:
        - bearerAuth: []
        - oauth2PasswordSSO: []
        - pkceSSO:
            - openid
      parameters:
        - in: path
          name: platformProviderUid
          required: true
          schema:
            type: string
        - in: query
          name: _filters
          required: false
          schema:
            type: string
        - in: query
          name: _sort
          required: false
          schema:
            type: string
        - in: query
          name: _order
          required: false
          schema:
            type: string
        - in: query
          name: page
          required: false
          schema:
            format: double
            type: number
        - in: query
          name: limit
          required: false
          schema:
            format: double
            type: number
servers:
  - url: http://localhost:5001/api/v1
