# Control Plane

## Checklist
- Keycloak is running on http://localhost:5980
- Keycloak realm `controlplane` configured with SMTP credentials
- Keycloak client `controlplane` configured within realm `controlplane`
- Keycloak user `admin@saas.internal` configured in realm `controlplane`
